import Component from "@glimmer/component";

export default class RentalImageComponent extends Component {
  constructor(...args) {
    super(...args);
    /* initialize */
    this.isLarge = false;
  }
}
